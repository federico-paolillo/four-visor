# Goal progress

Goal session: `2b10ee6edbe5535061cfe6bf`

The Worker traced the failed calibration artifact to rootless UID remapping on the host bind mount. A stopped-container tmpfs was tested and rejected because its contents are discarded. The replacement path uses a task-scoped Docker named volume initialized through ordinary rootless Docker, keeps the hardened backend at UID 65532 with a read-only root filesystem, and exports the finished artifact through a transient helper. An end-to-end probe verified non-root writing and a host-readable, codex-owned exported file without privileged mode, system changes, or published ports.

The corrected one-hour calibration (`1h-r2`) is starting sequentially. The original one-hour bind-mount attempt remains useful timing/failure evidence and will be retained as a separate failed raw row in `docs/STATS.md`; it will not be silently replaced by the rerun.
