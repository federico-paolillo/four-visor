# Goal progress

Goal session: `2b10ee6edbe5535061cfe6bf`

The first 50 minutes of the sequential four-hour candidate (`4h-a`) completed with zero error records and no competing acquisition. The run passed its initial capacity check without the warning seen in both one-hour attempts, remains rate-bound under the unchanged `1s/10/5s/2/1s` acquisition policy, and is using approximately 150 MiB with 15 processes/threads.

No evidence currently implicates the retry, concurrency, request-timeout, or rate defaults. The Worker is continuing the same candidate unchanged so its terminal timing, size, and content row remains comparable with the one-hour calibration.
