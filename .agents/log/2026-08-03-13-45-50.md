# Goal progress

Goal session: `2b10ee6edbe5535061cfe6bf`

The Worker implemented the provisional configurable-deadline path and started the first sequential live `snapshot` calibration with a one-hour deadline in the hardened backend image. Early queue telemetry reported 11,423 runnable threads and 3,520 remaining rate-start slots. At the deadline, 7,951 threads were classified at the lineage rate-deadline stage; the only observed non-deadline live-data failures were seven thread `404` responses and one catalog `404`. This reinforces a roughly 3-hour-10-minute rate floor and keeps four hours as the first plausible candidate.

The acquisition completed without overlapping another run, but final snapshot creation failed because the rootless container could not write `/results/attempt-1h.json` through the task-specific bind mount. Timing and failure-class evidence remain useful, while serialized size and content counts are missing. The Worker is handling the mount-permission error and must repeat the calibration after verifying output writability; no system configuration, privileged container, published port, or firewall change is authorized.

The user added a durable evidence requirement during this interval. `docs/STATS.md` will contain one raw row per completed experiment with policy, architecture/emulation status, timing, outcome, serialized size, content/state totals, failure counts, units, and limitations; bulky raw artifacts remain outside the repository.
